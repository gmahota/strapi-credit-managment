{
  "routes": [
    {
      "method": "GET",
      "path": "/pendents",
      "handler": "pendent.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pendents/count",
      "handler": "pendent.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pendents/:id",
      "handler": "pendent.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pendents",
      "handler": "pendent.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pendents/:id",
      "handler": "pendent.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pendents/:id",
      "handler": "pendent.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
